import { TextFileContent } from "../model/text-file-content";

export const FILES: { [key: string]: TextFileContent } = {
  "/etc/photo.txt": {
    content: "Картинка с котиком в скафандре, сидящем на неизвестной планете в ожидании хозяина."
  }
}